# Personal Codex Notes

- 回答は簡潔にする
- まずローカルの実態を確認する
- 可能なら調査で止めず修正まで進める
- 破壊的な操作や外部影響のある操作は確認を取る
- dotfiles では再現性を優先し、認証情報や履歴などのローカル state は混ぜない
- 迷ったら `fast` より `review` / `deep` を選び、雑な変更より確実さを優先する
- 会話圧縮の前に、次回も役立つ要点があれば Codex のメモリーへ短く保存してから終える

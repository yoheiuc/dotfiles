# 判断ログ アーカイブ

L1 / L2 の「整合性ルール」「ツール採用基準」等の live rule とは別に、過去の個別判断（採用 / 却下 / 覆された記録）を時系列で残す集約先。L2 が毎ターン読まれる context を圧縮するための保管場所であり、原本としての権威は無い（コードと L1 / L2 の現行運用が真）。

2026-04-27 以降は `adopted` / `rejected` / `superseded` を区別せず、判断はすべて本ファイル末尾に直接追記する（L2 にインラインの判断ログは置かない）。`status` 列で区別する。

| 日付 | status | 判断 | 理由 | 関連 commit/issue |
|---|---|---|---|---|
| 2026-04 | rejected | `chrome-devtools-mcp` plugin を採用しない | 実 Chrome に attach せず throwaway Chromium を spawn する設計で、`pwattach` 運用と相反 | `2064181` (playwright-cli: adopt attach --cdp=chrome) |
| 2026-04-25 | adopted | Exa は HTTP MCP のまま、plugin 化しない | `claude-plugins-official` の `exa` plugin は stdio 版で API key と subprocess が要る → policy「remote MCP > local stdio MCP」と衝突。`web_search_exa` / `web_fetch_exa` の 2 tool で日常用途は足りる | `b21fdcd` (consolidate web search on Exa) |
| 2026-04-25 | rejected | `code-review` / `pr-review-toolkit` plugin を採用しない | `/review` `/ultrareview` `/security-review` で同等の用途をカバー済み | `b2ad19e` (install claude-plugins-official) |
| 2026-04-25 | rejected | `code-simplifier` plugin を採用しない | 同等の `simplify` skill を vendoring 済み | `b2ad19e` |
| 2026-04-25 | rejected | `commit-commands` plugin を採用しない | HEREDOC + Co-Authored-By 等の独自コミット規約と衝突しやすい | `b2ad19e` |
| 2026-04-25 | rejected | `superpowers` / `context7` plugin は dotfiles 管理外 | 必要時にセッション内 `/plugin install` で個別投入。dotfiles 必須セットには含めない | 本ファイルに明文 |
| 2026-04-25 | rejected | `shfmt` を Brewfile に追加しない | `shellcheck` で実害は止まる。personal repo で style 揺れの実害なし。L2 policy「迷ったら削除」に該当 | （本セッションでの判断、commit なし） |
| 2026-04-25 | adopted | Jamf 公式 docs MCP (`https://developer.jamf.com/mcp`) を baseline 化、`jamf-mcp-server` (実 tenant 操作) は保留 | Jamf Pro を業務利用しているが、まず無認証 read-only な公式 docs MCP で API 仕様検索を加速。実 tenant 操作 MCP は credential + Keychain wrapper の運用判断（read-only mode 可否、最小権限 API client 発行）を経てから次セッションで判断 | 本セッション |
| 2026-04-25 | superseded | 業務 SaaS の MCP（Netskope / Microsoft MCP for Enterprise / Jamf 実 tenant / Intune）は dotfiles baseline に乗せず手順だけ集約 | tenant URL / access code / API token が tenant 固有で共有不可。`mcp-with-keychain-secret` wrapper の framework は維持、実 install は手順を見て user 判断 | 本セッション、L2 「仕事用 MCP の setup 手順」セクション |
| 2026-04-25 | rejected | `revise-claude-md` slash command を Stop hook 化しない（手動 `/revise-claude-md` 運用のまま） | Stop hook 適性 = ①LLM 不要 ②冪等 ③高速 ④user approval 不要、の 4 条件。`chezmoi-auto-apply` は全部満たす（chezmoi apply は決定論的 sync）が、`revise-claude-md` は LLM 必須・reflection 含む・step 5 で approval 要求の 5 ステップ command。Stop で LLM を回すと再帰問題もある（hook 内 session の Stop でまた hook…）。`auto-save` は memory rewrite で similar 構造だが context >75% の rate-limit があるから成立している | claude-plugins-official の `claude-md-management/commands/revise-claude-md.md` を確認、本セッション |
| 2026-04-25 | rejected | `session-report` plugin を baseline 化しない（必要時に bundle 内 `analyze-sessions.mjs` を直接叩く） | 集計粒度は `subagent_type` 単位で、`Agent({model: ...})` 別 breakdown を持たないため subagent モデル振り分け効果の検証という当初目的にはギャップ。週次〜月次 review 用途は `node ~/.claude/plugins/marketplaces/claude-plugins-official/plugins/session-report/skills/session-report/analyze-sessions.mjs --json --since 7d` で足りる（plugin install すら不要）。baseline 化の閾値「毎回欲しいか」を満たさない | 本セッションで実行・確認 |
| 2026-04-25 | superseded | `hookify` plugin を per-user で install、dotfiles `.claude/hookify.*.local.md` に 4 rule 配置（trial、結論ペンディング） | rule ファイルは `.local.md` 慣例で gitignored（`.gitignore` に `.claude/hookify.*.local.md` 追加済み）。block: `rm -rf $HOME\|/`, `git push --force`。warn: `git reset --hard`, `--no-verify` / `--no-gpg-sign`。pretooluse.py は try/except で常に exit 0 = エラー時も操作を block しない安全側。**Claude 再起動で活性化**。次セッションで rule 誤発火 / 漏れを観察してから baseline 化（`scripts/lib/claude-plugins.sh` の配列追加）を判断。cwd 依存設計のため他 project に展開するなら template 化が要る | 本セッションで install + rule 配置 |
| 2026-04-26 | adopted | `presentation` skill を vendor 追加して Office 3 点セット (.docx/.xlsx/.pptx) + .pdf を揃える。SKILL.md は自作（既存 `doc` / `spreadsheet` と同じトーン、python-pptx + LibreOffice rendering） | `anthropics/skills@pptx` は LICENSE で「retain copies outside the Services」「create derivative works」が禁止なので vendor 不可。`claude-plugins-official` marketplace にも `pptx` plugin は無く、`openai/skills` の `.curated/` 旧 set からも消えていた。community skill (`tfriedel/...` / `claude-office-skills/...`) は配布元の継続性が読めない。**既存 `doc` / `spreadsheet` の flavor で minimal な SKILL.md を自作** が license 問題ゼロ・lineage 統一・依存も既存の python-* + LibreOffice + Poppler に揃う最善解 | 本セッション |
| 2026-04-26 | rejected | Auto Mode (`--permission-mode auto`) は dotfiles baseline 化しない | Max / Team / Enterprise plan 限定の Research Preview で plan 依存。session 起動時の trigger なので config file に persist させる対象でなく、`/fewer-permission-prompts` で project-shared allowlist を整備する既存運用 (`5327658` 系列) と職能重複 | 本セッション |
| 2026-04-26 | rejected | Opus 4.7 の API 破壊的変更（fixed thinking budget unsupported / adaptive thinking off-default / temperature・top_p・top_k 非デフォルト 400 / tokenizer 1.0–1.35x / thinking content default omit）は dotfiles 範囲外 | Messages API (Anthropic SDK) の話で Claude Code 本体には影響しない。SDK code を書くときは `claude-api` skill が拾う | 本セッション |
| 2026-04-26 | adopted | "Exa for Claude" 調査の結論：別ブランディング製品は無く、現 baseline (`https://mcp.exa.ai/mcp` HTTP MCP) と同じ。ただし `?tools=web_search_exa,web_fetch_exa,web_search_advanced_exa` で **`web_search_advanced_exa`（domain / date / category filter）を baseline 化**（前 baseline は default 2 tool のみ） | URL parameter は filter モードで、3 tool 列挙すると defaults + advanced の 3 つすべて有効。advanced は技術調査で `site:` / 日付絞り込み相当の精度向上に効く。代替案だった (a) Exa stdio MCP (`exa-mcp-server`) は L2 既存判断「remote > stdio」で rejected、(b) Exa が公式配布する 6 種の Claude Code skill (Code Search / Research Paper 等) は配布が **copy-paste のみ** で marketplace / CLI 経路を持たず L2「skill 配布優先順位」のいずれにも乗らないため不採用 | 本セッション、Exa 公式 docs 確認 |
| 2026-04-26 | adopted | `scripts/ai-secrets.sh` 一式と `~/.local/bin/ai-secrets` wrapper を全削除（Brave API Key prompt orphan の解消） | Brave Search MCP は 2026-04 に retire 済だが secret 入力 apparatus（`scripts/ai-secrets.sh` + `home/dot_local/bin/executable_ai-secrets` + `tests/ai-secrets.sh` + `tests/ai-secrets-wrapper.sh` + `Makefile` target + `scripts/lib/ai-config.sh` の 3 helper）が残置。唯一の入力フィールドが Brave API Key だったので「prompt だけ抜く」と無目的スクリプトの dead code 化 → 一式削除。`mcp-with-keychain-secret` 本体（generic wrapper）と `tests/ai-repair.sh` / `tests/ai-audit.sh` の brave-search anti-regression fixtures は残す（既存マシンの自動 cleanup を保証） | 本セッション、L2「迷ったら削除」適用 |
| 2026-04-26 | adopted | `mcp-with-keychain-secret` framework と `docs/work-mcp-setup.md` を全削除（API key 必須かつ未使用の業務 MCP apparatus 整理） | 2026-04-25 の判断ログでは「framework として残置、必要時に手順参照」としていたが、user 確認で Netskope / Microsoft MCP for Enterprise / Jamf 実 tenant / Intune の **全 4 候補が「使ってない or いつか使うレベル」**で consumer ゼロ。「迷ったら削除」+ zero consumer = full removal。Brave anti-regression fixtures は test data の文字列 path のみ参照で実体不要のため温存（既存マシンの brave-search 自動 cleanup は維持）。将来 API key 必須 MCP が要るときは `security add-generic-password` 直接 + `claude mcp add` で都度組む（CLAUDE.md L2 / docs にテンプレを残さない、本当に必要になった時にその場で書く） | 本セッション、Phase 2 整理 |
| 2026-04-26 | adopted | `home/dot_claude/dot_mcp.json` を全削除（dead config 解消） | 公式 docs (https://code.claude.com/docs/en/mcp) で MCP scope は **local=`~/.claude.json`** / **project=`<project>/.mcp.json`** / **user=`~/.claude.json`** の 3 つだけ。`~/.claude/.mcp.json` はどの scope にも該当せず Claude Code は load しない。にも関わらず L2 管理表で「baseline はここ」と読める箇所が複数あり、実体（`~/.claude.json`、ai-repair 管理）と二重管理状態 → 「dot_mcp.json と ai-repair のどっちが真か」の判断ノイズが永続化。chezmoi source 削除 + ai-repair の orphan target 能動 rm 追加（Codex retire と同じパターン）+ L2 管理表 / 依存マップ / current-state.md の参照を実態（ai-repair single source of truth）に統一 | 本セッション |
| 2026-04-26 | superseded | `effortLevel: "xhigh"` を `~/.claude/settings.json` の dotfiles baseline に追加（旧 policy「local 自由・触らない」を覆す） | Opus 4.7 公式が「ほぼ全タスクで xhigh、最難関だけ max（max は overthinking）」と team-shareable な default を提示。旧 policy 制定時はこの「明確な共有可能 default」が無かった。`/effort` で local 上書きは可能（次の `make ai-repair` で snap back する rewrite-on-repair 方式）。`autoUpdatesChannel` と同じ運用に揃える。**翌 2026-04-27 に xhigh → high へ降格（routine task で overthink）**、L2 active row はその high 採用エントリ | 本セッション、Qiita "Opus 4.7でClaude Codeの使い方が180度逆になった" |
| 2026-04-27 | rejected | `hookify` plugin trial を rule 整理せず retire 確定（baseline 化見送り） | smoke test の結論：rm rule は permission 重複で値ゼロ、`git push --force` block / `--no-verify` warn は permission を抜ける真の防御線、だが hookify は (1) cwd 相対設計で `.claude/hookify.*.local.md` を cwd から読む = dotfiles repo 内でしか発火しない、(2) 規約名 `.local.md` が gitignore convention で source-of-truth + commit と相性悪く template 化コストが value に見合わない。retire 後、push --force / no-verify を本当に止めたくなったら `home/dot_claude/hooks/` に独自 PreToolUse hook を直接書く（cwd 横断発火 + dotfiles 文化的にも筋）。trial 残骸（rule files / 万一 install された plugin）は ai-repair で能動 cleanup、ai-audit で legacy 検知 | 本セッション、L2 pending L110 を archive へ移動 |
| 2026-04-25 | adopted | `ai-repair.sh` の hooks block は merge せず wholesale 置換のまま | Claude Code は `settings.json` と `settings.local.json` の hooks を append/concat で merge する（override ではない）ので、baseline を全置換しても user-added hook は失われない。user-added は `settings.local.json` 側に置く運用 | https://code.claude.com/docs/en/hooks.md |
| 2026-04-25 | adopted | `.claude/settings.json` に project-shared な permission allowlist を commit | `fewer-permission-prompts` skill の出力を採用。read-only / inert なものに絞り、書き込み・任意コード実行・retired MCP は除外。`settings.local.json` (gitignored) は machine-local override として残す | `5327658` 系列で追加、本セッション |
| 2026-04-26 | adopted | Codex retire の cleanup 漏れを解消（`home/dot_codex/` 削除 + `ai-repair.sh` で `~/.codex` 能動削除） | 2026-04 に Codex / Gemini 廃止を宣言した（`docs/notes/current-state.md`）が `home/dot_codex/skills/.../__pycache__/autosave_memory.cpython-314.pyc` の orphan 1 ファイルが残置 → chezmoi が `~/.codex/` を毎 sync 再生成 → `make ai-audit` が「retired agent state still on disk」warn を出し続ける状態。frontend-design 廃止と同じパターン（source 削除 + ai-repair で能動 rm）で収束させた | 本セッション |
| 2026-04-27 | adopted | `effortLevel` baseline を `xhigh` → `high` に降格 | user 確認で routine task に対して xhigh は overthink 寄りと判断（Opus 4.7 公式 default である xhigh は「最難関で max を避ける」基準で設計されており、routine 用途では high で十分）。`xhigh` を要する難タスクは `/effort xhigh` で都度上書きできる（次の `make ai-repair` で `high` に snap back）。snap-back 方式そのものは維持 | 本セッション |
| 2026-04-27 | adopted | `Bash(git push origin main)` / `Bash(git push origin HEAD)` を project-shared allow に追加 | 本 dotfiles は personal repo で contributor 1 名・PR 必須なし・branch protection なし、main への direct push が常用フロー。Claude Code 2.1.x の LLM 判断ベース deny が「default branch への push」を session ごとに違う wording で block しており、user の `push してください` 指示でも override されない。allow の exact match が "ask 階層の `Bash(git push*)` を最具体的にカバー" + "LLM judge が wording を変えても無効化" の二重に効く。dotfiles 自体は「push が共有 publication」なのでこの allow をリポ管理にするのが筋（個人マシン依存にしない） | 本セッション、Claude Code permission 公式 docs |
| 2026-04-26 | rejected | プロジェクト側 Stop hook + test 自動実行は dotfiles baseline 化しない（project repo の責務） | `~/.claude/settings.json` の Stop hook は dotfiles 自体の運用 (`auto-save` / `chezmoi-auto-apply`) 専用。記事推奨の `npm test` 自動再実行型 hook は project ごとに test runner / file pattern が違うので project repo の `.claude/settings.json` 側で組む。dotfiles はそのための allowlist (`Bash(make *)` 等) は既に project-shared `.claude/settings.json` に commit 済 | 本セッション、`5327658` 系列 |
| 2026-04-26 | adopted | `home/dot_claude/commands/` を 18 → 3 → 0 本に整理（**全削除**、`commands/` ディレクトリも削除） | E refactor で 3 本残す判断をしたが、user から「コマンド系使ったことない」signal を受けて再評価：slash command は `/<name>` を user が打って初めて Claude が読む = 打たないなら **dead code**。`/perf` `/research` は dead code として削除。`/playwright` の dotfiles 固有部分（CDP attach 時のセキュリティ guardrail）は本来 user invoke を待たず常に守るべき内容なので、**L1 CLAUDE.md の `## ブラウザ自動化のセキュリティ規則（CDP attach 時）` 節としてインライン化**（毎ターン読まれる context へ移動 = dead code から live rule へ昇格）。orphan target は `ai-repair.sh` の retired_command ループで能動削除。`tests/commands.sh` は consumer 消失で削除。 | 本セッション、L1 `43b4fa7` ルール適用 |
| 2026-04-26 | adopted | `security-best-practices` skill を vendor → upstream-install (`npx skills add` from `tech-leads-club/agent-skills`) に移行 | dotfiles 最大の重量物（10 言語別 reference 計 8,005 行 = skills/ subtree の 76%）が「skill 配布優先順位」 tier 3 (vendor) に居座っていた。upstream が `npx skills add` 互換と判明 → tier 2 (upstream CLI 配布) に昇格可能。`gws` / `find-skills` / `notion-cli` と同じ install pattern を `post-setup.sh` に追加。chezmoi は orphan target を自動削除しないため `ai-repair.sh` で legacy vendored copy を能動 rm（marker file `.upstream-installed` 不在で legacy 判定、frontend-design retire と類似パターン） | 本セッション、`tech-leads-club/agent-skills` 確認 |
| 2026-04-26 | adopted | `ui-ux-pro-max` skill を vendor → upstream-install (`nextlevelbuilder/ui-ux-pro-max-skill` v2.5.0) に移行 | 同じ tier-2 pattern。vendored は SKILL.md 658 行 + 16 CSV データファイル + 3 Python script で計 ~1500 行。upstream は active maintenance（2026-03-10 v2.5.0）かつ `npx skills add` 互換。security-best-practices と同じ marker-file 機構で legacy vendored copy 判定 + 自動 re-install | 本セッション、`nextlevelbuilder/ui-ux-pro-max-skill` 確認 |
| 2026-04-26 | adopted | `frontend-design` plugin を baseline 化（`CLAUDE_GENERAL_PLUGINS` に追加） | 2026-04-24 commit `c606583` で vendor 退却したまま、ローカル install / baseline 配列の drift 状態が放置されていた。user が「直近 3-6 ヶ月で Web frontend を書く」確認 → A: baseline 化を選択。`ui-ux-pro-max`（DB 型）と `frontend-design`（aesthetic guideline 型）は厳密に重複しないので併用可 | 本セッション、`scripts/lib/claude-plugins.sh` 編集 |
| 2026-04-26 | adopted | `microsoft-docs` plugin を baseline 化（`CLAUDE_GENERAL_PLUGINS` に追加） | reverse drift（installed_plugins.json には install 済 / `CLAUDE_GENERAL_PLUGINS` 配列に未記載）を frontend-design 対応中に発見。user が「Microsoft tech を扱う」確認 → baseline 化。`microsoft-docs` (search / fetch) / `microsoft-code-reference` (SDK 検証) / `microsoft-skill-creator` (新規 skill scaffolding) の 3 skill を提供。Azure / .NET / M365 / Windows / Power Platform 全般の docs grounding に使う | 本セッション、`scripts/lib/claude-plugins.sh` 編集 |
| 2026-04-26 | adopted | `sequential-thinking` MCP の管理を `post-setup.sh`（一回 install）から `ai-repair.sh`（drift detect-and-repair）+ `ai-audit.sh`（verify）に移管 | 他 4 baseline MCP（vision / exa / jamf-docs / slack）は upsert + verify の対をなすが、`sequential-thinking` だけ「post-setup 一回 install、以降 audit blind」の例外状態。registration が消失しても `make ai-audit` は気付かない = drift 検知の死角。L2 「依存マップ：MCP サーバー追加時は ai-repair / ai-audit / tests を全部更新」原則からも逸脱していた。`ai_config_mcp_registration_state` helper は唯一の consumer 消失で zero-consumer dead code になったため同時除去 | 本セッション |
| 2026-04-26 | adopted | `.claude/settings.json` allowlist に 6 件追加（`Bash(playwright-cli *)` を broad 化、`Bash(npm run dev)` / `Bash(npm test)` exact、`Bash(gitleaks *)`、Gmail MCP 2 種）。既存 `playwright-cli {close,resize,screenshot,goto,open,eval}` 6 entry は残す | `/fewer-permission-prompts` の集計で `--raw snapshot` (52) / `-s=foo eval` (49) / `snapshot` (31) 等が prompt 源泉と判明。L1 の CDP attach guardrails（実 Chrome attach 時の対象範囲制限）が真の防御線で、allowlist 広さと直交するため broad 化で安全側を崩さない。`npm run dev` / `npm test` は **exact のみ**（`npm run *` だと package.json scripts 経由で arbitrary code execution に拡がる skill rule 違反） | 本セッション、`/fewer-permission-prompts` 結果 |
| 2026-04-26 | adopted | Bash から呼べる汎用 CLI 7 種（`gh` / `playwright-cli` / `mmdc` / `marp` / `jq` / `yq` / `pandoc` / `rg` / `ffmpeg`）を L1 ツール選択ルール表で明示「使える前提」化、Brewfile に `marp-cli` / `ffmpeg` を追加 | 8/9 は既に Brewfile or post-setup で install 済だったが L1 表に未掲載のものは Claude が「install されてるか分からないので使わない」分岐を取りがち（`which` で都度確認するだけムダ）。L1 は毎ターン context に乗る = 表に書けば「前提」として常時効く（slash command と違い invoke 不要）。`marp` / `ffmpeg` だけ未 install だったので Brewfile に追加し install と表記載を対で揃えた（L2「依存マップ：CLI 系ツールの追加」運用通り）。`rg` は Claude 内蔵 Grep tool が裏で使うので「Bash 経由で直接叩く必要がある時用」と注記して棲み分け | 本セッション、Claude Code 勉強会 2026-04 スライド p.40 |
| 2026-04-27 | adopted | `ai-repair.sh` が repo-local `${REPO_ROOT}/.serena/` も能動削除（commit `3a35e47` の `~/.serena` 対応の続き） | Serena MCP 廃止後の残骸は `~/.serena`（runtime cache + memories）と repo-local `.serena/`（Serena が project context として書く副産物）の 2 箇所に出る。`3a35e47` で前者は片付けたが後者は手付かずだった = 「Serena の retire は完了」と読めて実は 1 箇所漏れている drift 状態。`.gitignore` 済みで `git status` には出ないので発見がさらに遅れる。ai-repair で両方掃除する方が `ai-audit` / 手動 `ls` 上のノイズ源を完全に消せる | 本セッション |
| 2026-04-27 | adopted | vendor 4 skill (`doc` / `pdf` / `presentation` / `spreadsheet`) を Anthropic 公式 `anthropic-agent-skills` marketplace の `document-skills` plugin (`docx`/`pdf`/`pptx`/`xlsx` を bundle) に移行 | dotfiles の vendor 6 skill のうち 4 種が **Anthropic が公式 marketplace plugin として配布**し始めた。配布優先順位 tier-1 (marketplace) に上がれた時点で tier-3 (vendor) は劣後。同じ「vendor 退却」パターンは `frontend-design` (commit `c606583`) / `security-best-practices` / `ui-ux-pro-max` で既に確立、ただし今回は配布経路が `npx skills add` (tier-2) ではなく `claude plugin install <name>@<marketplace>` (tier-1) で 1 段上。`jupyter-notebook` / `screenshot` の 2 種は `anthropic-agent-skills` marketplace に存在しない（`document-skills` / `example-skills` / `claude-api` の 3 plugin に分かれた skill 列に該当なし）ので tier-3 vendor が最終手段として残置。`scripts/lib/claude-plugins.sh` を複数 marketplace 対応に拡張（`CLAUDE_DOCUMENT_PLUGINS` 追加、`claude_plugin_is_installed` / `claude_plugins_check_summary` を marketplace 引数化）、`scripts/post-setup.sh` を `_install_claude_marketplace_plugins` ヘルパーで dedup、ai-repair に vendor 4 path の能動 rm 追加 | 本セッション、`.claude-plugin/marketplace.json` 確認 |
| 2026-04-27 | adopted | `ai-repair.sh` の MCP 5 ブロック（vision / sequential-thinking / exa / jamf-docs / slack）を private helper `_upsert_http_mcp` / `_upsert_stdio_mcp` 2 種に集約 | 同じ「現在値 read → expected と比較 → drift なら upsert + ok / なければ ok already」のパターンが ~50 行で 5 回展開されており、6 個目の baseline MCP を足すコスト + コピペ時の typo 経由 drift 余地が大きかった。HTTP 系 (url 比較) と stdio 系 (command + `\|`-joined args 比較) の 2 variant に分けて関数化、call site は 1 行 / 1 MCP に縮約 (~50 行 → ~10 行)。helper は ai-repair.sh ローカル定義に留め、`scripts/lib/ai-config.sh` の純粋 mutation surface は維持（L2「既存 lib を肥大化させない」原則）。`tests/ai-repair.sh` の clean / drift / legacy 3 scenario と `make ai-audit` で動作確認済 | 本セッション |
| 2026-04-27 | adopted | `doctor.sh` の `chezmoi diff` チェックを exit code 分岐に変更 | 旧コードは `diff_out=$(chezmoi diff 2>&1 \|\| true)` で exit code を捨て、出力が非空かどうかだけで分岐していた。`chezmoi diff` は差分があっても exit 0 を返す仕様なので `\|\| true` 自体は意図的だが、**chezmoi 本体が壊れた場合**（corrupt config 等）も非空 stderr を出すため「unapplied changes detected」と誤報する。`if diff_out=$(...)` で exit code 取り、非ゼロなら `fail` + 出力 dump にする方が原因切り分けが速い。`tests/doctor.sh` の happy path はそのまま green | 本セッション |
| 2026-04-27 | adopted | `bootstrap.sh` の smoke test (`tests/bootstrap.sh`) を追加 | 新マシン provisioning の入口にも関わらず regression test が無く、drift は user 報告でしか発覚しない状態だった。フル integration test は実 Brewfile + 実 Xcode CLT を要するので、`xcode-select` / `swift` / `brew` / `chezmoi` を PATH-stub で「installed / success」に固定し、bootstrap の no-op convergence path（log 出力 / `brew bundle --file=<repo>/home/dot_Brewfile` 呼び出し / chezmoi symlink 作成 / `chezmoi apply` 呼び出し / stale symlink の repoint）を検証。`tests/post-setup.sh` 既存パターンと同じ stub 方式で再現性あり。Makefile `test:` ターゲットに追加 | 本セッション |
| 2026-04-27 | adopted | L1 (`home/dot_claude/CLAUDE.md`) に「MCP baseline 5 本」一覧を追加 | 毎ターン context に乗る L1 に MCP baseline (vision / exa / slack / jamf-docs / sequential-thinking) の一覧が無く、「何が登録されているはず」を知るには L2 + `ai-repair.sh` を読む必要があった。L1 はユーザー / agent が常時参照する live rule の置き場で、L2 は dotfiles 編集ルール（depender 視点）。MCP baseline は前者寄りの情報なので L1 にも 1 ブロック持たせる。token / OAuth 詳細は L1 に持ち込まず Claude Code + ai-repair.sh コメントに留める | 本セッション |
| 2026-04-27 | adopted | L1 / L2 / archive の context cost を整理 — L2 から「個別判断ログ」表を削除して archive に一本化（L2 側は link 1 行）、scripts/lib 責務一覧表を 1 段落に圧縮、commit 規約は L1 に集約、「このファイルの読み方」「Claude Code slash command の追加・削除」を圧縮、L1 の重複 bullet を統合 | L1 は毎ターン全プロジェクトで context 消費 / L2 も dotfiles 配下で context 消費。両方が live rule に直接寄与しない reference 情報（過去判断ログ / コードから derive 可能な lib 責務 / L1-L2 pointer 重複）で肥大化。「live rule だけ残す」方針で L2 229→123、archive +20 行（L1 は同節で policy 追加と相殺、別エントリ参照） | 本セッション |
| 2026-04-27 | adopted | L1 に「ブラウザ自動化の運用デフォルト」節を新設（Edge 専用 binary / `--headed --persistent --profile=$HOME/.ai-edge`、navigation 後 Edge 前面化、状態変更系のみ事前 narration、`playwright_actions.log` 追記、禁止 click 正規表現、禁止 eval / run-code 一覧） | 既存の「CDP attach 時のセキュリティ規則」は実 Chrome に attach した場合の guardrail だったが、AI 用の通常運用ブラウザを **何にするか** / **どう振る舞うか** が L1 に明文化されておらず、毎セッション暗黙の運用に頼っていた。Edge を AI 専用 binary にすると主 Chrome と Dock / Cmd+Tab が分離し、bundled Chromium の Cloudflare 弾き / Chrome 136+ の `--remote-debugging-port` 拒否も回避できる。run-time に効く live rule なので毎ターン読まれる L1 が正しい置き場。L1 行数は本節追加分（~20 行）と上記圧縮分が相殺し 119 → 129 で純増 | 本セッション |
| 2026-04-27 | adopted | Chrome CDP attach 系 (`pwattach` / `pwdetach` / `PLAYWRIGHT_AI_CHROME_READY`) を全面撤去し、AI 用ブラウザは Edge 専用 binary 1 系統に統一。`pwedge` zsh helper（`--browser=msedge --headed --persistent --profile`）と `playwright-cli` shell wrapper（state-changing コマンドを `~/.cache/playwright-cli/actions.log` に TSV 追記）を新設。L1 「CDP attach 時のセキュリティ規則」節を一般化（Edge 運用に readjust）、L2 のツール採用基準表から `pwattach` 行を削除、README / SECURITY / post-setup onboarding / doctor / ai-repair / ai-audit のコメント・onboarding 表現を Edge 系に書き換え。tests/playwright-zsh.sh の Chrome attach test #10-13 を pwedge / log wrapper test に置換（XDG_CACHE_HOME を tmpdir に固定して副作用隔離）。Brewfile に `cask "microsoft-edge"` を追加（既存マシンの drift 解消） | Chrome 経由の AI ブラウザ運用は実態として使われず、`pwattach` の前提（`chrome://inspect/#remote-debugging` toggle / Chrome 144+ / 専用プロファイル env var ready）を毎マシンで揃える運用負荷が、得られる隔離レベル（≒ Edge 専用 profile と同等）に見合わなくなった。Edge 1 系統に絞ることで「session 値が `chrome` か否かで分岐」する L1 規則も unify でき、L1 が短く強くなる。ログ wrapper を tooling 側に持たせたのは、L1 の「全 goto / click / eval を追記」規約を AI の narration 任せにせず机上で再現可能にするため（`command playwright-cli` で bypass 経路は残す） | 本セッション |
| 2026-04-27 | adopted | `playwright-cli` shell wrapper に 3 種の機械 guard を追加（A: 禁止 click / dblclick / press 正規表現、B: 禁止 eval / run-code 書き込みパターン、D: state-changing で session 未設定なら exit 1）。pwedge 起動成功後に `tab-list` を 1 度叩いて非 AI profile 兆候 (`gmail` / `admin.google` / `chase` / `stripe.*dashboard` 等) が見えたら stderr warning（C: profile 異常検知、block しない）。bypass は既存の `command playwright-cli ...` 経路を踏襲。tests/playwright-zsh.sh に test #13–15 + benign / read-only counterexample を追加 | L1 「ブラウザ自動化のセキュリティ規則」「運用デフォルト」節は禁止 click 正規表現 / 禁止 eval / state-changing narration を要求するが、実装は actions.log 追記と profile 強制のみで、禁止 pattern は **AI の自制依存**だった（外部 prompt-injection や指示の見落としで素通りする構造）。同じ regex を wrapper の case 分岐に降ろすことで、L1 の規約をテキストから tooling 層へ昇格。session guard (D) は「`PLAYWRIGHT_CLI_SESSION` 未 export = cold start」を可視化し、`pwedge` / `pwsession` を踏まずに毎回 370MB profile を再 load する遅さの主因を運用の入口で潰す（headless 化は L1「user が画面で追えるように」と矛盾するので採らない）。bypass を `command playwright-cli` のみに限定したことで、「禁止 pattern を意図的に通したいときの 1 経路」を明示し AI の判断ノイズを減らす | 本セッション |
| 2026-04-27 | adopted | `tests/playwright-zsh.sh` の `env PATH=… zsh -c …` 系 callsite（16 箇所）を `env -i "${HERMETIC_BASE_ENV[@]}" … zsh -c …` ベースに hermetic 化。`HOME` / `PATH` / `XDG_CACHE_HOME` / `TERM` / `LANG` / `LC_ALL` だけを明示的に渡す配列を line 30 付近に定義し、test 固有 env (`PW_STUB_EXIT` / `PLAYWRIGHT_AI_EDGE_PROFILE` / `PLAYWRIGHT_CLI_SESSION=test`) は配列の後に key=val で後置。L2 30 秒節から workaround 注釈（`env -u PLAYWRIGHT_CLI_SESSION make test` で回避、恒久対策未対応）を削除 | 親 shell が `pwedge` で `PLAYWRIGHT_CLI_SESSION=edge` を export した状態で `make test` を回すと line 85 (pwlogin negative assertion `${PLAYWRIGHT_CLI_SESSION:-<unset>}`) が leak で fail する問題の恒久対策。test を環境変数の有無に依存しない設計に倒すことで、interactive 開発中に毎回 `env -u` を覚える運用負荷を消す。inline `PATH=… zsh -c "source …"` 形式の 4 callsite (line 31 / 48 / 54 / 59) は leak 経路ではなく、env -i 化で zsh 起動 env を見落とす副作用リスクの方が大きいため対象外 | 本セッション |
| 2026-04-28 | rejected | **rebrowser-patches Phase 1**（`@playwright/cli` の bundled playwright を patch する case）を適用不能と判明したため見送り。Anthropic 配布の `@playwright/cli` 0.1.8 は `playwright-core 1.60.0-alpha-2026-04-14` を `lib/coreBundle.js` 1 本に webpack 風 bundle してあり、rebrowser-patches v1.0.19 が前提とする line-based unified diff（`lib/server/chromium/crConnection.js` 個別 file targeting）では `"Internal error, patch command cannot find file to patch"` で fail する | 当初計画は research agent の `npm view @playwright/cli` ベースで「Microsoft thin wrapper（依存 `playwright` 1 本）で patch 可」前提だったが、実体（`/opt/homebrew/lib/node_modules/@playwright/cli/node_modules/playwright-core/lib/coreBundle.js`）と `npx rebrowser-patches@latest patch --packageName playwright-core` の実行で incompatible と確認。Phase 2 候補は (a) **patchright drop-in 置換**（`patchright` library で thin CLI を自前実装）、(b) **coreBundle.js への手書き shim**（bundle 内 `Runtime.Enable` 周辺を sed/regex で書き換える post-install patch、fragile）。bot 判定が業務影響レベルに来るまで Phase 2 は着手しない（同日の adopted entry で `cli.config.json` 経由の Phase 0 stealth が成立した結果、現状ニーズは満たされた） | 本セッション、実体確認 + 実行 fail |
| 2026-04-28 | adopted | **playwright-cli stealth を `~/.playwright/cli.config.json` (chezmoi 管理) 経由で実現**。`launchOptions.args=["--disable-blink-features=AutomationControlled"]` と `ignoreDefaultArgs=["--enable-automation"]` を global config に入れて、playwright-cli が起動毎に auto load し `chromium.launchPersistentContext` の launchOptions にそのまま流し込む。CLI 表面・wrapper・テストに一切触らず stealth が成立。bot.sannysoft.com で `WebDriver(New) missing (passed)` / `navigator.webdriver === false` を実機確認済み。整合: `home/dot_playwright/cli.config.json` source、`scripts/lib/claude-checks.sh` の `playwright_is_stealth_patched` predicate (`jq` で必須キー検証)、`scripts/doctor.sh` の warn ベース surface、L2 「`~/.claude/` 配下の管理モード」表に `~/.playwright/cli.config.json` 行追加 | 上記 rebrowser-patches Phase 1 が bundle 構造と非互換と判明したあと、coreBundle.js を読んで `cliArgs` が `--config` を取る + `defaultConfig` の `browser.launchOptions` がそのまま `launchPersistentContext` に spread される + `~/.playwright/cli.config.json` が global config として auto load される、の 3 点を確認。Microsoft Playwright が標準でサポートする `launchOptions` interface だけで `navigator.webdriver` 抑止が達成できるため、最小工数 + 標準準拠 + chezmoi 管理に乗る形で stealth を成立させた。Runtime.Enable leak まで塞ぐ Phase 2 (patchright / coreBundle shim) は bot 判定の実害が出てから着手 | 本セッション、`coreBundle.js` L60430 / L63818 / L64801 確認 + `bot.sannysoft.com` 実機検証 |
| 2026-04-28 | adopted | **`pwedge` を tag 駆動の `pwopen <tag> [url]` に汎用化**。`home/dot_config/zsh/playwright.zsh` で `pwopen` を主関数化（profile path = `$HOME/.ai-<tag>` の sibling 規則、env override は `PLAYWRIGHT_AI_<TAG_UPPER>_PROFILE`、tag の `-` は env 名上 `_` に変換）、`pwedge` は `pwopen edge "$@"` の back-compat shim に退化。tab-list 汚染 guard は **tag=edge のときだけ発火** に絞り、SaaS テナント profile（Stripe / Salesforce 等が正常状態）での false-positive を回避。`tests/playwright-zsh.sh` に Test 17–20 を追加（custom-tag / hyphen→underscore env 解決 / 非 edge tag では tab-list 不発火 / usage error）。L1 ブラウザ自動化節を `<tag>` 化、L2 / README / cheat も追従 | SaaS マルチテナント並走需要（複数テナント間で cookie / localStorage を `~/.ai-edge` 1 個に同居させると AI 専用 profile 隔離前提が崩れる）。`pwedge2` のような ad-hoc 関数では 3 つ目以降にスケールしないので、tag 駆動 launcher + sibling profile 規則で N 個並走を 0 行コード変更で生やせる構造に倒した。env 命名規則 `PLAYWRIGHT_AI_<TAG_UPPER>_PROFILE` は `tag=edge → PLAYWRIGHT_AI_EDGE_PROFILE` で既存 env と完全一致するため後方互換ゼロ変更。tab-list guard は本来「user の main profile が AI 専用 profile を汚染している兆候」検出が目的なので、テナント profile では誤検知になる構造的不整合があり edge tag 限定が正しい。**rejected**: (a) `~/.ai-edge/<tag>/` nested 配置 → root が profile か container か曖昧、Edge user-data-dir 意味と衝突、(b) 全 tag に同 guard → SaaS で false-positive 連発し guard 形骸化、(c) `pwprofile` 命名 → navi cheat の `pw*` prefix 整列が崩れる | 本セッション |
| 2026-04-29 | adopted | **Bitwarden を CLI 路線で採用** — Brewfile に `brew "bitwarden-cli"`、zsh wrapper (`home/dot_config/zsh/bitwarden.zsh`) で **read-only allowlist を機械 enforce**（`list` / `get` / `generate` / `status` / `sync` / `unlock` / `lock` / `login` / `logout` / `config` / `completion` / `update` / `help` 以外は exit 1 + `~/.cache/bitwarden-cli/actions.log` に DENY 追記、bypass は `command bw …`）。tier-3 vendor SKILL.md (`home/dot_claude/skills/bitwarden-cli/SKILL.md`) で禁止行為（vault 値の third-party POST / `eval` 注入 / BW_SESSION の disk 永続化 / master password を user に求める / autonomous bulk read）を明文化。L1 (`home/dot_claude/CLAUDE.md`) に「Bitwarden CLI 操作のセキュリティ規則」節を新設し playwright と並列の運用規範に。doctor に `bw` セクション追加（version / serverUrl / Bitwarden.app 確認）、`tests/bitwarden-zsh.sh` で allowlist 13 case / denylist 14 case / bypass / hermetic env-leak を回帰。**MCP server (`@bitwarden/mcp-server`) は不採用** — 上流コミュニティスレッド (community.bitwarden.com 2025-07-10) が "considered a proof of concept and not suitable for production use" と明言、かつ local stdio (npx) で L2 ツール採用基準の tier 2「remote HTTP MCP」に該当しない。BW_SESSION は **`bwunlock` で current shell に短命 export する運用**で disk 永続化禁止（`.zshenv.local` / `.envrc` / Keychain いずれも不可）、長期 background agent から触りたい要件が出たら別計画で再評価。スコープは user 確認で **read-only + bitwarden.com 公式 SaaS** に確定（state-changing は user が `command bw …` で個別 bypass する運用で十分カバー） | user に「AI に Bitwarden を操作させたい、CLI 入れて AI 用にカスタマイズできるか調べてほしい」と依頼を受け、L2「ツール採用基準」マトリクス（tier 1: CLI + skill > tier 2: remote MCP > tier 3: vendor）と上流 POC ステータスを照合した結果、CLI 路線が一択と判明。BW_SESSION = vault 復号 master capability であり、L1 ブラウザ自動化規則と同レベル（または以上）の prompt-injection / credential exfiltration リスク評価が必要なため、blast radius を read-only に絞ることで「外部コンテンツ由来の悪意命令で `bw delete` / `bw export` が走る」経路を構造的に塞ぐ。allowlist 機械 enforce は L1「Bitwarden CLI 操作のセキュリティ規則」を AI の自制依存ではなく tooling 層に降ろす（playwright wrapper の禁止 click / 禁止 eval 設計と同パターン）。L2「変更箇所の依存マップ」に「allowlist 変更時は wrapper / SKILL.md / tests / L1 規則文面の 4 点同期」明記で drift 予防 | 本セッション、community.bitwarden.com 2025-07-10 thread、`@bitwarden/cli` Homebrew formula / npm `@bitwarden/mcp-server` README 確認 |
